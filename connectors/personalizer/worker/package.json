{
  "name": "ks-personalizer-api",
  "version": "0.1.0",
  "private": true,
  "description": "KiddieSketch Personalizer API — Cloudflare Worker for /upload-art (Phase 1) and /generate-sketch (Phase 2).",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:staging": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "secrets:set": "echo 'Run: wrangler secret put SUPABASE_URL; wrangler secret put SUPABASE_SERVICE_ROLE_KEY; wrangler secret put HASH_SALT'"
  },
  "devDependencies": {
    "wrangler": "^3.75.0"
  }
}

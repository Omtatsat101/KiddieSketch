node_modules/
.DS_Store
.env
.env.local
*.log

# Shopify theme files pulled locally via `shopify theme pull` (not committed)
# Only ignore the root-level /theme dir, NOT connector theme artifacts.
/theme/
/_working-theme/

# Worker secrets (template .dev.vars.example IS committed)
.dev.vars
